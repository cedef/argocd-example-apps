apiVersion: apps/v1
kind: Deployment
metadata:
  name: stacapi
  labels:
    env: {{ .Values.env }}
    component: database
spec:
  replicas: {{ .Values.replicas }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: stacapi
  template:
    metadata:
      labels:
        app: stacapi
        env: {{ .Values.env }}
        project: {{ .Values.projName }}
        componentName: {{ .Values.componentName }}
      annotations:
        accessible-from-internet: {{ .Values.openToInternet }}
    spec:
      containers:
      - image: alpine:latest
        name: alpine-shell
        command: ["/bin/sh", "-c", "sleep 3600"]
        ports:
          - name: http
            containerPort: 80
            protocol: TCP
        resources: {{- toYaml .Values.resources }}
